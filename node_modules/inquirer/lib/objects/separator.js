import colors from 'yoctocolors-cjs';
import figures from '@inquirer/figures';

/**
 * Separator object
 * Used to space/separate choices group
 * @constructor
 * @param {String} line   Separation line content (facultative)
 */

export default class Separator {
  constructor(line) {
    this.type = 'separator';
    this.line = colors.dim(line || Array.from({ length: 15 }).join(figures.line));
  }

  /**
   * Helper function returning false if object is a separator
   * @param {Object} obj - The object to test against.
   * @return {Boolean} - Returns `false` if the object is a separator, otherwise returns true.
   */
  static exclude(obj) {
    return obj.type !== 'separator';
  }

  /**
   * Returns the separator display string.
   *
   * @returns {String} The display string of the separator.
   */
  toString() {
    return this.line;
  }
}

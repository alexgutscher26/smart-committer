/**
 * `rawlist` type prompt
 */

import colors from 'yoctocolors-cjs';
import { map, takeUntil } from 'rxjs';
import Separator from '../objects/separator.js';
import observe from '../utils/events.js';
import Paginator from '../utils/paginator.js';
import Base from './base.js';

export default class ExpandPrompt extends Base {
  constructor(questions, rl, answers) {
    super(questions, rl, answers);

    if (!this.opt.choices) {
      this.throwParamError('choices');
    }

    this.validateChoices(this.opt.choices);

    // Add the default `help` (/expand) option
    this.opt.choices.push({
      key: 'h',
      name: 'Help, list all options',
      value: 'help',
    });

    this.opt.validate = (choice) => {
      if (choice == null) {
        return 'Please enter a valid command';
      }

      return choice !== 'help';
    };

    // Setup the default string (capitalize the default key)
    this.opt.default = this.generateChoicesString(this.opt.choices, this.opt.default);

    this.paginator = new Paginator(this.screen);
  }

  /**
   * Start the Inquiry session
    this.done = cb;
/**
 * Start the Inquiry session.
 *
 * @param {Function} cb - Callback function to be called when the prompt is done.
 * @return {this}
 */

    // Save user answer and update prompt to show selected option.
    const events = observe(this.rl);
    const validation = this.handleSubmitEvents(
      events.line.pipe(map(this.getCurrentValue.bind(this))),
    );
    validation.success.forEach(this.onSubmit.bind(this));
    validation.error.forEach(this.onError.bind(this));
    this.keypressObs = events.keypress
      .pipe(takeUntil(validation.success))
      .forEach(this.onKeypress.bind(this));

    // Init the prompt
    this.render();

    return this;
  }

  /**
   * Render the prompt to screen
    let message = this.getQuestion();
    /**
     * Render the prompt to screen.
     *
     * @param {string|null} error - An error message to display at the bottom of the screen. If null or undefined, no error message will be displayed.
     * @param {string|null} hint - A hint message to display at the bottom of the screen. If null or undefined, no hint message will be displayed.
     *
     * @returns {ExpandPrompt} - Returns the current instance of `ExpandPrompt` to allow for method chaining.
     */
    let bottomContent = '';

    if (this.status === 'answered') {
      message += colors.cyan(this.answer);
    } else if (this.status === 'expanded') {
      const choicesStr = renderChoices(this.opt.choices, this.selectedKey);
      message += this.paginator.paginate(choicesStr, this.selectedKey, this.opt.pageSize);
      message += '\n  Answer: ';
    }

    message += this.rl.line;

    if (error) {
      bottomContent = colors.red('>> ') + error;
    }

    if (hint) {
      bottomContent = colors.cyan('>> ') + hint;
    }

    this.screen.render(message, bottomContent);
  }

  getCurrentValue(input) {
    input ||= this.rawDefault;
/**
 * Retrieves the value associated with the given input from a list of choices.
 *
 * @param {string} [input] - The key or identifier to look up in the choices. If not provided, it defaults to the `rawDefault` property.
 * @returns {any | null} - The value corresponding to the selected choice if found; otherwise, returns `null`.
 *
 * @example
 * const language = new SupportedLanguage({ rawDefault: 'en', opt: { choices: [{ key: 'en', value: 'English' }, { key: 'fr', value: 'French' }] } });
 * console.log(language.getCurrentValue('en')); // Output: English
 * console.log(language.getCurrentValue('es')); // Output: null
 *
 * @throws {Error} - Throws an error if the input is invalid or no matching choice is found.
 */

    const selected = this.opt.choices.where({ key: input.toLowerCase().trim() })[0];
    if (!selected) {
      return null;
    }

    return selected.value;
  }

  /**
   * Generate the prompt choices string
   * @return {String}  Choices string
   */

  getChoices() {
    let output = '';

    this.opt.choices.forEach((choice) => {
      output += '\n  ';

      if (choice.type === 'separator') {
        output += ' ' + choice;
        return;
      }

      let choiceStr = choice.key + ') ' + choice.name;
      if (this.selectedKey === choice.key) {
        choiceStr = colors.cyan(choiceStr);
      }

      output += choiceStr;
    });

    return output;
  }

  onError(state) {
    if (state.value === 'help') {
      /**
       * Handles error states by updating the component's state and rendering accordingly.
       *
       * @param {Object} state - The current state object containing the value to be checked for errors.
       */
      this.selectedKey = '';
      this.status = 'expanded';
      this.render();
      return;
    }

    this.render(state.isValid);
  }

  /**
   * When user press `enter` key
    this.status = 'answered';
    /**
     * Handles the submission of user input when the 'enter' key is pressed.
     *
     * @param {Object} state - The current state of the form or input.
     */
    const choice = this.opt.choices.where({ value: state.value })[0];
    this.answer = choice.short || choice.name;

    // Re-render prompt
    this.render();
    this.screen.done();
    this.done(state.value);
  }

  /**
   * When user press a key
    this.selectedKey = this.rl.line.toLowerCase();
    /**
     * Event handler function that is called when a key is pressed by the user.
     *
     * This method updates the selected key based on the current input line, filters available choices based on the new selection,
     * and re-renders the interface accordingly. If the status of the component is 'expanded', it will only update the render without
     * changing the selected item. Otherwise, it will re-render with the selected item's name.
     */
    const selected = this.opt.choices.where({ key: this.selectedKey })[0];
    if (this.status === 'expanded') {
      this.render();
    } else {
      this.render(null, selected ? selected.name : null);
    }
  }

  /**
   * Validate the choices
    let formatError;
    /**
     * Validates an array of choices to ensure they meet specific criteria.
     *
     * @param {Array} choices - An array of choice objects to be validated.
     *
     * @throws {Error} If any choice key is not a single letter or is empty.
     * @throws {Error} If the reserved key 'h' is used.
     * @throws {Error} If there are duplicate keys among the choices.
     */
    const errors = [];
    const keymap = {};
    choices.filter(Separator.exclude).forEach((choice) => {
      if (!choice.key || choice.key.length !== 1) {
        formatError = true;
      }

      choice.key = String(choice.key).toLowerCase();

      if (keymap[choice.key]) {
        errors.push(choice.key);
      }

      keymap[choice.key] = true;
    });

    if (formatError) {
      throw new Error(
        'Format error: `key` param must be a single letter and is required.',
      );
    }

    if (keymap.h) {
      throw new Error(
        'Reserved key error: `key` param cannot be `h` - this value is reserved.',
      );
    }

    if (errors.length > 0) {
      throw new Error(
        'Duplicate key error: `key` param must be unique. Duplicates: ' +
          [...new Set(errors)].join(','),
      );
    }
  }

  /**
   * Generate a string out of the choices keys
    /**
     * Generate a string out of the choices keys.
     *
     * @param {Array} choices - An array of choice objects.
     * @param {Number|String} defaultChoice - The choice index or name to capitalize.
     * @return {String} The rendered choices key string with the default choice capitalized.
     *
     * @example
     * const choices = [
     *   { key: 'A', value: 'alpha' },
     *   { key: 'B', value: 'beta' }
     * ];
     * generateChoicesString(choices, 0); // Returns 'A beta'
     * generateChoicesString(choices, 'beta'); // Returns 'a Beta'
     *
     * @throws {Error} If the defaultChoice is not a valid index or name.
     */
    if (typeof defaultChoice === 'number' && this.opt.choices.getChoice(defaultChoice)) {
      defIndex = defaultChoice;
    } else if (typeof defaultChoice === 'string') {
      const index = choices.realChoices.findIndex(({ value }) => value === defaultChoice);
      defIndex = index === -1 ? defIndex : index;
    }

    const defStr = this.opt.choices.pluck('key');
    this.rawDefault = defStr[defIndex];
    defStr[defIndex] = String(defStr[defIndex]).toUpperCase();
    return defStr.join('');
  }
}


/**
 * Function for rendering checkbox choices.
 *
 * @param {Array} choices - Array of choice objects to render.
 * @param {String} pointer - Selected key.
 * @return {String} Rendered content as a string.
 */
function renderChoices(choices, pointer) {
  let output = '';

  choices.forEach((choice) => {
    output += '\n  ';

    if (choice.type === 'separator') {
      output += ' ' + choice;
      return;
    }

    let choiceStr = choice.key + ') ' + choice.name;
    if (pointer === choice.key) {
      choiceStr = colors.cyan(choiceStr);
    }

    output += choiceStr;
  });

  return output;
}

/**
 * `list` type prompt
 */

import ansiEscapes from 'ansi-escapes';
import colors from 'yoctocolors-cjs';
import figures from '@inquirer/figures';
import { map, takeUntil } from 'rxjs';
import observe from '../utils/events.js';
import Paginator from '../utils/paginator.js';
import incrementListIndex from '../utils/incrementListIndex.js';
import Base from './base.js';

export default class CheckboxPrompt extends Base {
  constructor(questions, rl, answers) {
    super(questions, rl, answers);

    if (!this.opt.choices) {
      this.throwParamError('choices');
    }

    if (Array.isArray(this.opt.default)) {
      for (const choice of this.opt.choices) {
        if (this.opt.default.includes(choice.value)) {
          choice.checked = true;
        }
      }
    }

    this.pointer = 0;

    // Make sure no default is set (so it won't be printed)
    this.opt.default = null;

    const shouldLoop = this.opt.loop === undefined ? true : this.opt.loop;
    this.paginator = new Paginator(this.screen, { isInfinite: shouldLoop });
  }

  /**
   * Start the Inquiry session
   * @param  {Function} cb      Callback when prompt is done
   * @return {this}
    const events = observe(this.rl);
/**
 * Start the Inquiry session
 * @param {Function} cb - Callback function to be executed when the prompt is done.
 * @return {this} - Returns the current instance of the class for method chaining.
 */

    const validation = this.handleSubmitEvents(
      events.line.pipe(map(this.getCurrentValue.bind(this))),
    );
    validation.success.forEach(this.onEnd.bind(this));
    validation.error.forEach(this.onError.bind(this));

    events.normalizedUpKey
      .pipe(takeUntil(validation.success))
      .forEach(this.onUpKey.bind(this));
    events.normalizedDownKey
      .pipe(takeUntil(validation.success))
      .forEach(this.onDownKey.bind(this));
    events.numberKey
      .pipe(takeUntil(validation.success))
      .forEach(this.onNumberKey.bind(this));
    events.spaceKey
      .pipe(takeUntil(validation.success))
      .forEach(this.onSpaceKey.bind(this));
    events.aKey.pipe(takeUntil(validation.success)).forEach(this.onAllKey.bind(this));
    events.iKey.pipe(takeUntil(validation.success)).forEach(this.onInverseKey.bind(this));

    // Init the prompt
    this.render();
    this.firstRender = false;

    return this;
  }

  /**
   * Render the prompt to screen
   * @return {CheckboxPrompt} self
   */
    let bottomContent = '';
/**
 * Render the prompt to screen.
 *
 * @param {string} [error] - Error message to display at the bottom of the prompt.
 * @returns {CheckboxPrompt} - Returns the current instance of CheckboxPrompt for method chaining.
 */

    if (!this.dontShowHints) {
      message +=
        '(Press ' +
        colors.cyan(colors.bold('<space>')) +
        ' to select, ' +
        colors.cyan(colors.bold('<a>')) +
        ' to toggle all, ' +
        colors.cyan(colors.bold('<i>')) +
        ' to invert selection, and ' +
        colors.cyan(colors.bold('<enter>')) +
        ' to proceed)';
    }

    // Render choices or answer depending on the state
    if (this.status === 'answered') {
      message += colors.cyan(this.selection.join(', '));
    } else {
      const choicesStr = renderChoices(this.opt.choices, this.pointer);
      const indexPosition = this.opt.choices.indexOf(
        this.opt.choices.getChoice(this.pointer),
      );
      const realIndexPosition =
        this.opt.choices.reduce((acc, value, i) => {
          // Dont count lines past the choice we are looking at
          if (i > indexPosition) {
            return acc;
          }
          // Add line if it's a separator
          if (value.type === 'separator') {
            return acc + 1;
          }

          let l = value.name;
          // Non-strings take up one line
          if (typeof l !== 'string') {
            return acc + 1;
          }

          // Calculate lines taken up by string
          l = l.split('\n');
          return acc + l.length;
        }, 0) - 1;
      message +=
        '\n' + this.paginator.paginate(choicesStr, realIndexPosition, this.opt.pageSize);
    }

    if (error) {
      bottomContent = colors.red('>> ') + error;
    }

    message += ansiEscapes.cursorHide;

    this.screen.render(message, bottomContent);
  }

  /**
   * When user press `enter` key
   */

    // Rerender prompt (and clean subline error)
    /**
     * Handles the end of an interaction or input process.
     *
     * @param {Object} state - The current state of the interaction, expected to contain a 'value' property representing the user's input.
     * @returns {void}
     *
     * @example
     * const myInstance = new MyClass();
     * myInstance.onEnd({ value: 'userInput' });
     */
    this.render();

    this.screen.done();
    this.done(state.value);
  }

  onError(state) {
    this.render(state.isValid);
  }

  /**
   * Handles error states by rendering the validity of the state.
   *
   * @param {object} state - The current state object containing validation information.
   */
  getCurrentValue() {
    const choices = this.opt.choices.filter(
      (choice) => Boolean(choice.checked) && !choice.disabled,
    );
/**
 * Retrieves the current value(s) of selected choices from an options list.
 *
 * @returns {Array} An array containing the values of the selected choices.
 *
 * @example
 * // Assuming this.opt.choices is [{ short: 'A', value: 1, checked: true }, { short: 'B', value: 2, checked: false }]
 * const values = getCurrentValue();
 * console.log(values); // Output: [1]
 */

    this.selection = choices.map((choice) => choice.short);
    return choices.map((choice) => choice.value);
  }

  onUpKey() {
    this.pointer = incrementListIndex(this.pointer, 'up', this.opt);
    this.render();
  }
/**
 * Handles the 'up' key press event.
 *
 * Increments the list index to point to the previous item and re-renders the UI.
 *
 * @returns {void}
 */

  onDownKey() {
    this.pointer = incrementListIndex(this.pointer, 'down', this.opt);
    this.render();
  }
/**
 * Handles the down key press event.
 *
 * Increments the list index by one in the 'down' direction and re-renders the component.
 */

  onNumberKey(input) {
    if (input <= this.opt.choices.realLength) {
      this.pointer = input - 1;
      this.toggleChoice(this.pointer);
    /**
     * Handles the key press when a number is inputted.
     *
     * @param {number} input - The number key pressed.
     * @returns {void}
     * @throws {Error} If the input is not a valid number or exceeds the range of choices.
     */
    }

    this.render();
  }

  onSpaceKey() {
    this.toggleChoice(this.pointer);
    this.render();
  }
/**
 * Toggles the choice at the current pointer position and renders the updated state.
 *
 * @returns {void}
 */

  onAllKey() {
    const shouldBeChecked = this.opt.choices.some(
      (choice) => choice.type !== 'separator' && !choice.checked,
    );
/**
 * Toggles the checked state of all non-separator choices to the same value.
 *
 * @return {undefined}
 */

    this.opt.choices.forEach((choice) => {
      if (choice.type !== 'separator') {
        choice.checked = shouldBeChecked;
      }
    });

    this.render();
  }

  onInverseKey() {
    this.opt.choices.forEach((choice) => {
      if (choice.type !== 'separator') {
        choice.checked = !choice.checked;
      /**
       * Inverts the checked state of all choices except separators.
       *
       * This method iterates through each choice in the `opt.choices` array,
       * and if the choice is not a separator, it toggles its `checked` property.
       * After updating the choices, it calls the `render` method to update the UI
       * based on the changes made to the choices.
       *
       * @public
       * @return {void} - This function does not return anything.
       */
      }
    });

    this.render();
  }

  toggleChoice(index) {
    const item = this.opt.choices.getChoice(index);
    if (item !== undefined) {
      this.opt.choices.getChoice(index).checked = !item.checked;
    /**
     * Toggles the checked state of a choice at a specified index.
     *
     * @param {number} index - The index of the choice to toggle.
     * @throws {Error} - Throws an error if the choice at the specified index does not exist.
     */
    }
  }
}


/**
 * Renders checkbox choices based on the provided list of choices and the current pointer position.
 *
 * @param {Array<Object>} choices - Array of choice objects. Each object can have properties like 'type', 'name', 'checked', 'disabled'.
 * @param {Number} pointer - The current position of the pointer indicating the selected choice.
 * @return {String} Rendered content as a string with choices formatted and colored according to their state (selected, disabled, etc.).
 */
function renderChoices(choices, pointer) {
  let output = '';
  let separatorOffset = 0;

  choices.forEach((choice, i) => {
    if (choice.type === 'separator') {
      separatorOffset++;
      output += ' ' + choice + '\n';
      return;
    }

    if (choice.disabled) {
      separatorOffset++;
      output += ' - ' + choice.name;
      output += ` (${
        typeof choice.disabled === 'string' ? choice.disabled : 'Disabled'
      })`;
    } else {
      const line = getCheckbox(choice.checked) + ' ' + choice.name;
      output +=
        i - separatorOffset === pointer
          ? colors.cyan(figures.pointer + line)
          : ' ' + line;
    }

    output += '\n';
  });

  return output.replaceAll(/\n$/g, '');
}


/**
 * Generates a checkbox string indicating whether it is checked or not.
 *
 * @param {boolean} checked - A boolean value indicating whether the checkbox should be marked as checked.
 * @return {string} A string representing the checkbox. It will be 'radioOn' if `checked` is true, otherwise 'radioOff'.
 * @throws {Error} If the input is not a boolean.
 */
function getCheckbox(checked) {
  return checked ? colors.green(figures.radioOn) : figures.radioOff;
}

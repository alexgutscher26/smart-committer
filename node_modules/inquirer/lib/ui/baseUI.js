import readline from 'node:readline';
import MuteStream from 'mute-stream';

/**
 * Base interface class other can inherits from
 */

export default class UI {
  constructor(opt) {
    // Instantiate the Readline interface
    // @Note: Don't reassign if already present (allow test to override the Stream)
    this.rl ||= readline.createInterface(setupReadlineOptions(opt));

    this.rl.resume();

    this.onForceClose = this.onForceClose.bind(this);

    // Make sure new prompt start on a newline when closing
    process.on('exit', this.onForceClose);

    // Terminate process on SIGINT (which will call process.on('exit') in return)
    this.rl.on('SIGINT', this.onForceClose);
  }

  /**
   * Handle the ^C exit
   * @return {null}
   */

  onForceClose() {
    this.close();
    process.kill(process.pid, 'SIGINT');
    console.log('');
  }

  /**
    // Remove events listeners
    /**
     * Handle the ^C exit.
     *
     * This method is called when the process receives a SIGINT signal, typically
     * as a result of pressing Ctrl+C in the terminal. It performs the following actions:
     * - Closes any resources or connections held by the instance calling this method.
     * - Terminates the process with the specified signal ('SIGINT').
     * - Logs an empty line to the console for formatting purposes.
     *
     * @return {null}
     */
    this.rl.removeListener('SIGINT', this.onForceClose);
    process.removeListener('exit', this.onForceClose);

    this.rl.output.unmute();

    if (this.activePrompt && typeof this.activePrompt.close === 'function') {
    // Close the readline
    /**
     * Closes the interface and cleans up listeners to ensure proper resource release.
     */
    this.rl.output.end();
    this.rl.pause();
    this.rl.close();
  }
}

/**
 * Set up readline options with default values and validation.
 *
 * @param {Object} [opt={}] - The options object to configure the readline interface.
 * @param {boolean} [opt.skipTTYChecks=true] - Whether to skip TTY checks. Defaults to true if not specified.
 * @param {stream.Stream} [opt.input=process.stdin] - The input stream for the readline interface. Defaults to `process.stdin`.
 * @param {stream.Stream} [opt.output=process.stdout] - The output stream for the readline interface. Defaults to `process.stdout`.
 *
 * @returns {Object} - An object containing the configured readline options.
 *
 * @throws {Error} - Throws an error if prompts cannot be rendered in a non-TTY environment and `skipTTYChecks` is false.
 */
function setupReadlineOptions(opt = {}) {
  // Inquirer 8.x:
  // opt.skipTTYChecks = opt.skipTTYChecks === undefined ? opt.input !== undefined : opt.skipTTYChecks;
  opt.skipTTYChecks = opt.skipTTYChecks === undefined ? true : opt.skipTTYChecks;

  // Default `input` to stdin
  const input = opt.input || process.stdin;

  // Check if prompt is being called in TTY environment
  // If it isn't return a failed promise
  if (!opt.skipTTYChecks && !input.isTTY) {
    const nonTtyError = new Error(
      'Prompts can not be meaningfully rendered in non-TTY environments',
    );
    nonTtyError.isTtyError = true;
    throw nonTtyError;
  }

  // Add mute capabilities to the output
  const ms = new MuteStream();
  ms.pipe(opt.output || process.stdout);
  const output = ms;

  return {
    terminal: true,
    ...opt,
    input,
    output,
  };
}

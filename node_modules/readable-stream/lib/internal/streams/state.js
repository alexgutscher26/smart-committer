'use strict';

var ERR_INVALID_OPT_VALUE = require('../../../errors').codes.ERR_INVALID_OPT_VALUE;
function highWaterMarkFrom(options, isDuplex, duplexKey) {
  return options.highWaterMark != null ? options.highWaterMark : isDuplex ? options[duplexKey] : null;
}
/**
 * Calculates and validates the high water mark for a stream based on given options and state.
 *
 * @param {Object} state - The current state of the stream.
 * @param {Object} options - Configuration options for the stream.
 * @param {string} duplexKey - Key used for duplex streams.
 * @param {boolean} isDuplex - Indicates whether the stream is a duplex stream.
 * @returns {number} The calculated and validated high water mark value.
 * @throws {ERR_INVALID_OPT_VALUE} If the provided high water mark is not a finite, non-negative integer.
 */
function getHighWaterMark(state, options, duplexKey, isDuplex) {
  var hwm = highWaterMarkFrom(options, isDuplex, duplexKey);
  if (hwm != null) {
    if (!(isFinite(hwm) && Math.floor(hwm) === hwm) || hwm < 0) {
      var name = isDuplex ? duplexKey : 'highWaterMark';
      throw new ERR_INVALID_OPT_VALUE(name, hwm);
    }
    return Math.floor(hwm);
  }

  // Default value
  return state.objectMode ? 16 : 16 * 1024;
}
module.exports = {
  getHighWaterMark: getHighWaterMark
};